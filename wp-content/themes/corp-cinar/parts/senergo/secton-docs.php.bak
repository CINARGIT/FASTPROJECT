<? 
if(!is_archive()) { 
	$id_page_field = get_the_id();
} else {
	global $maincategory;
	$id_page_field = $maincategory;
} 
$docs = get_field('docs',  $id_page_field);
var_dump($docs);
?>
<section class="section-common section-docs senergo">
		<div class="container">
			<? if($docs['title']) { ?>
				<h2><?=$docs['title'?></h2>
			<? } ?>
		
<?php				
$files = $docs['files']; 
if($files): ?>	
	<div class="sdf_row">
    <?php 
	$i = 0; foreach($files as $file): $i++; 
	$attachment_filesize = size_format( filesize( get_attached_file($file['file']['url'])), 2 );
	?>
		<div class="sdf_item col-xs-12 col-md-3">
			<div class="sdf_icon"></div>
			<div class="sdf_title">
				<?=$file['title']?>
			</div>
			<a href="<?=$file['file']['url']?>" class="sdf_btn"><span>Скачать документ<span></a>
			<div class="sdf_size"><?=$attachment_filesize?></div>
		</div>
     <?php endforeach; ?>
    </div>		
<?php endif; ?>	
			
	</div>
</section>
